#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int num;
	char cchar;

	for (num = '0'; num <= '9' num++)
		putchar(num);
	for (cchar = 'a'; cchar <= 'f'; cchar++)
		putchar(cchar);
	putchar('\n');

	return (0);
}
